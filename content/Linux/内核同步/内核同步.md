---
tags: [linux, kernel-locking]
type: index
---

> 内核同步是 Linux 内核中保护共享数据免受并发访问的机制集合。内核开发者需要面对来自多个 CPU、中断处理程序、软中断以及进程上下文的并发访问，选择合适的同步原语是保证正确性和性能的关键。

## 核心概念

| 概念 | 说明 |
|------|------|
| 临界区 | 访问共享资源的代码段，需要互斥保护 |
| 竞态条件 | 多个执行流对共享资源的非原子操作导致的结果不确定 |
| 死锁 | 两个或多个执行流互相等待对方持有的锁，永远阻塞 |
| 锁顺序 | 获取多个锁时必须按固定顺序，避免死锁 |
| 可重入 | 同一执行流能否重复获取同一把锁 |

## 同步机制全景对比

| 机制 | 上下文 | 可睡眠 | 自旋等待 | 重入 | 适用场景 |
|------|--------|--------|----------|------|----------|
| spinlock | 任意 | ❌ | ✅ (忙等) | ❌ (不可重入) | 中断/软中断保护的短临界区 |
| raw_spinlock | 任意 (更早期) | ❌ | ✅ | ❌ | 实时内核、早期启动代码 |
| mutex | 进程 | ✅ | ❌ | ❌ | 长临界区、需要睡眠 |
| semaphore | 进程 | ✅ | ❌ | ✅ (计数) | 资源计数 (有限数量) |
| rwlock | 任意 | ❌ | ✅ | ❌ | 读多写少、短临界区 |
| rwsem | 进程 | ✅ | ❌ | ❌ | 读多写少、长临界区 |
| RCU | 任意 | 读端: ❌ / 写端: ✅ | ❌ | ✅ | 读极多写极少、链表遍历 |
| atomic_t | 任意 | ❌ | ❌ (无锁) | ✅ | 简单计数器、标志位 |

## 文档导航

* [spinlock](spinlock.md) — 自旋锁原理与变体
* [mutex](mutex.md) — 互斥锁语义与实现
* [RCU](RCU.md) — 读 - 复制 - 更新机制详解
* [信号量](信号量.md) — 计数信号量
* [原子操作](原子操作.md) — atomic_t / atomic64_t API
* [读写锁](读写锁.md) — rwlock / rwsem
* [内存屏障](内存屏障.md) — smp_rmb / smp_wmb / smp_mb

## 选择正确的锁: 决策流程

```
临界区很短 (< 几微秒)?
├─ YES → 在中断或软中断上下文中?
│        ├─ YES → spinlock 或 raw_spinlock
│        └─ NO  → spinlock 足够
└─ NO  → 需要保护读多写少的数据?
         ├─ YES → 读者只需要遍历 (不需写)?
         │        ├─ YES → RCU (无锁读)
         │        └─ NO  → rwlock (自旋) 或 rwsem (睡眠)
         └─ NO  → 使用 mutex (进程上下文标准选择)
```

## Lockdep (死锁检测)

内核内置的锁依赖验证器。使用时只需在 .config 中启用：

```
CONFIG_LOCKDEP=y
CONFIG_PROVE_LOCKING=y
```

Lockdep 在运行时追踪所有锁的获取顺序，检测：

| 问题 | 含义 |
|------|------|
| AA 死锁 | 同一任务重复获取同一把锁 |
| ABBA 死锁 | 两个任务以不同顺序获取锁 |
| 锁在中断上下文中未关中断 | 在中断中获取了仅在进程上下文持有的锁 |
| 锁依赖反转 | 违反锁顺序约束 |

Lockdep 通过在 `/proc/lockdep_chains` 输出锁依赖图来帮助调试。

## 相关资料

* Linux 内核源码: `include/linux/spinlock.h`、`kernel/locking/`、`include/linux/rcupdate.h`
* Linux Kernel Development (Robert Love) — 第 7-10 章
* `Documentation/locking/` — 内核锁文档
* `CONFIG_LOCKDEP` — 锁依赖验证
* `/proc/lockdep` — 锁依赖信息
