---
tags: [linux, syscall, calling-convention, vdso]
type: entity
aliases:
  - Linux 系统调用约定
  - VDSO
---

> 系统调用约定定义了用户程序如何将参数传给内核，以及内核如何将结果返回。从 `int 0x80` 到 `syscall/sysret`，调用机制的演进显著降低了系统调用的开销。VDSO 更进一步，将部分高频系统调用提速至无需陷入内核。

## 调用约定

### x86_64 (syscall 指令)

| 寄存器 | 用途 |
|--------|------|
| `rax` | 系统调用号 |
| `rdi` | 第 1 个参数 |
| `rsi` | 第 2 个参数 |
| `rdx` | 第 3 个参数 |
| `r10` | 第 4 个参数 (注意：不是 rcx) |
| `r8` | 第 5 个参数 |
| `r9` | 第 6 个参数 |
| 返回值 | `rax` (0 成功，负值 = -errno) |

### x86 (int 0x80 传统方式)

| 寄存器 | 用途 |
|--------|------|
| `eax` | 系统调用号 |
| `ebx` | 第 1 个参数 |
| `ecx` | 第 2 个参数 |
| `edx` | 第 3 个参数 |
| `esi` | 第 4 个参数 |
| `edi` | 第 5 个参数 |
| 返回值 | `eax` |

## 调用机制演进

| 机制 | 架构 | 速度 | 备注 |
|------|------|------|------|
| `int 0x80` | x86 (32-bit) | 慢 | 传统中断门方式，需查 IDT |
| `sysenter/sysexit` | x86 (32-bit) | 快 | Intel 引入的快速系统调用 |
| `syscall/sysret` | x86_64 | 最快 | AMD 设计，Intel 后续支持 |

### int 0x80 (传统方式)

```
用户态执行 int 0x80
    → CPU 从 IDT 查找 0x80 号门描述符
    → 切换到内核栈 (TSS 中读取)
    → 保存 CS:EIP/SS:ESP 到内核栈
    → 跳转到 entry_INT80_32
    → 在 sys_call_table 中查找处理函数
    → 执行完毕后 iret 返回用户态
```

### syscall (现代 x86_64)

```
用户态执行 syscall 指令
    → CPU 从 MSR 中读取:
       LSTAR     → 内核入口地址
       STAR      → 代码段选择子
       SF_MASK   → RFLAGS 屏蔽掩码
    → RFLAGS 存入 R11, 返回地址存入 RCX
    → 切换到内核栈 (IST 或 per-CPU 栈)
    → 跳转到 entry_SYSCALL_64
    → 执行系统调用
    → sysret 返回: RCX → RIP, R11 → RFLAGS
```

> `int 0x80` 在 x86_64 上仍可使用（通过 IA-32 仿真），但性能远不如 `syscall`。现代应用不应使用 `int 0x80`。

## VDSO (Virtual Dynamic Shared Object)

VDSO 是内核映射到每个用户进程地址空间的一小段代码，使得某些系统调用无需陷入内核即可完成。

| 函数 | VDSO 加速 | 说明 |
|------|-----------|------|
| `gettimeofday()` | ✅ 通过 rdtsc 读取时间，无 syscall | 最常用 VDSO 场景 |
| `clock_gettime()` | ✅ 通过 VDSO 实现 | |
| `time()` | ✅ | |
| `getcpu()` | ✅ 读取当前 CPU 编号 |

```bash
# 查看 VDSO 在进程中的映射
cat /proc/self/maps | grep vdso
# 输出: 7ffe9d5b4000-7ffe9d5b6000 r-xp 00000000 ... [vdso]
```

vsyscall 是 VDSO 的前身，映射在固定地址 `0xffffffffff600000`，现已逐步被 VDSO 取代。

## 直接调用 syscall 示例

### 使用 syscall() 函数 (推荐)

```c
#include <unistd.h>
#include <sys/syscall.h>
#include <stdio.h>
#include <errno.h>

int main()
{
    // 直接调用 SYS_getpid (系统调用号 39)
    pid_t pid = syscall(SYS_getpid);
    printf("PID: %d\n", pid);

    // 直接调用 SYS_write (系统调用号 1)
    const char *msg = "Hello via syscall!\n";
    syscall(SYS_write, 1, msg, 19);

    // 错误处理: 返回 -1 并设置 errno
    long ret = syscall(99999); // 不存在的 syscall
    if (ret == -1) {
        printf("Error: %s\n", strerror(errno));
    }

    return 0;
}
```

### 使用内联汇编 (x86_64)

```c
#include <unistd.h>

static inline long my_getpid(void)
{
    long ret;

    __asm__ volatile (
        "mov $39, %%rax\n"    // syscall 号: SYS_getpid
        "syscall\n"           // 调用 syscall 指令
        : "=a" (ret)          // 输出: rax → ret
        :
        : "rcx", "r11",       // syscall 会修改 rcx, r11
          "memory"
    );

    return ret;
}

int main()
{
    printf("PID: %ld\n", my_getpid());
    return 0;
}
```

## 相关文档

- [系统调用](系统调用.md) — 系统调用总览
- [系统调用表](系统调用表.md) — 系统调用号列表与 sys_call_table
- [ptrace 与 strace](ptrace与strace.md) — 系统调用跟踪

## 相关资料

- Linux 内核源码: `arch/x86/entry/`
- [Linux man pages — syscall(2)](https://man7.org/linux/man-pages/man2/syscall.2.html)
- [Linux man pages — vdso(7)](https://man7.org/linux/man-pages/man7/vdso.7.html)
